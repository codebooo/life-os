<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Transparent, non-animated window so the share chooser floats over the app
        the text was shared from. Compose draws the card itself.
    -->
    <style name="Theme.LifeOs.ShareOverlay" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
