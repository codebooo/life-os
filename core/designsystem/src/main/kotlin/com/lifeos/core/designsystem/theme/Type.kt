package com.lifeos.core.designsystem.theme

import androidx.compose.material3.Typography

/**
 * M3 typography scale (§7.1). The system font on modern Android resolves to
 * Roboto Flex; tabular figures are applied per-composable where numbers align
 * (amounts, clock digits, timers) rather than globally.
 */
internal val LifeOsTypography = Typography()
